{
  "extends": ["//"],
  "tasks": {
    "build": {
      "passThroughEnv": ["DATABASE_URL", "DIRECT_URL", "SKIP_ENV_VALIDATION"]
    },
    "db:generate": {
      "cache": false,
      "passThroughEnv": ["DATABASE_URL", "DIRECT_URL"]
    }
  }
}
